const Footer = () => {
  // await new Promise((resolve) => setTimeout(resolve, 2000));
  console.log("Footer rendered");
  return (
    <footer className="bg-amber-100 p-[1rem]">
      <p>Footer</p>
    </footer>
  );
};

export default Footer;
